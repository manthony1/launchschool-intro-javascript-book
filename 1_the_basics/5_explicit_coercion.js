//Refactor the code from the previous exercise to use explicit coercion, so it logs 15 instead.



let explicitCoercion = parseInt('5') + 10;
console.log(explicitCoercion);
console.log(typeof(explicitCoercion));

